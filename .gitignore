# Archivos de configuración y temporales del sistema operativo
.DS_Store
Thumbs.db

# Dependencias del gestor de paquetes (si se usaran en el futuro)
/node_modules

# Archivos de log
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Archivos de build
/dist
/build

# Credenciales y claves de API (MUY IMPORTANTE)
# Si en el futuro se usara una API Key que no sea gestionada por el entorno de Canvas,
# y se almacena localmente, se debería añadir aquí.
# Por ejemplo:
# .env
# .env.local
# *.env
# *.key
# config.js # Si contiene claves sensibles
